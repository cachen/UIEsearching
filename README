Usage: searching.py [options]
For example: searching.py -a gedit -t findAll -c PushButton -n Undo -C 0
Options:
  -h | --help        Print help information (this message)
  -a | --appname     Give application's name
  -t | --findtype    'findAll' or 'find'
  -c | --ctrlname    Give the control's name you want to find
  -n | --elementname Give the element's name you want to find
  -C | --children    Whether print out element's children

